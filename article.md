说实话，我以前怕的剪辑环节不是“剪”，是重复。

想创意这种事我还能忍，真正让我想摔鼠标的，是把同一套片头片尾往几十个视频里塞。还有一回，电脑跑了一晚上脚本，第二天起来发现参数写错，全白干。

后来我在 Win 11 上试着让 Codex 帮我干这些，试了几天，感觉能省不少事。

这篇不聊玄乎的技术，就聊我实际怎么用它，顺便把能照抄的流程和踩过的坑一起放这儿。

![自动化剪辑流程](images/pic1.png)

## 为什么我放弃了手动剪辑

先交代一下背景：剪映和 PR 我都用过，功能没什么问题，我的问题在于，同样的动作要做太多次。

我每周要处理一批口播素材。每段要去的片头片尾位置基本固定，输出格式也一样，但我还是得一遍遍拖时间线、选参数、等导出。剪一条还好，剪十条就麻了。

参数这种小事也特别容易忘。上次用的码率是多少，这次是不是该选 1080p，导出到哪个文件夹，全靠脑子记。脑子一乱，画面糊了、文件找不着，又得重来。

所以我后来想明白了一件事：我不需要更会剪，我需要有人帮我把重复的部分跑完。

> **别一上来就全量跑，先拿一个文件试。**

## Codex 到底在帮我干什么

简单说：**它帮我写脚本、跑命令，把重复动作变成流水线。**

我也不是程序员，看不懂那些复杂的代码，所以我只让 Codex 帮我干脏活累活。它不替我做审美，也不替我看片子，但它能把我烦的那部分——重复操作，接过去。

我只需要说清楚三件事：**素材在哪、要做什么、输出到哪。**

## 我平时让它剪这些

- **批量截片段**：按时间点切掉多余内容
- **去片头片尾**：统一剪掉固定开场和结尾
- **批量转格式**：转成统一的 H.264、1080p
- **自动拼接**：多个片段按顺序合成一条
- **批量加字幕、片尾、水印**：整批处理，不用一个个来

这些活背后大多是 Python 加 FFmpeg。你不用会写脚本，把需求说成人话就行。

## 我常用的工具链

除了 Codex 和 FFmpeg，我还会顺手用几个小工具：

- **Everything**：按文件名快速搜素材，不用一层层翻文件夹
- **Snipaste**：截图记参数，上次用的分辨率、码率，截一张图放着
- **资源监视器**：系统自带，转码时看 CPU 占用和硬盘读写，判断是不是卡死了

工具都不贵，也不用都装。关键是让 Codex 跑的时候，我能快速确认它到底在干什么。

还有一个很没技术含量但很有用的细节：为了跑批量转码，我专门给笔记本配了个散热底座。风扇狂转的时候，至少不会因为过热降频。我也把素材盘重新分了个区，输入输出分开，找文件方便多了。

## Win 11 上先准备三样东西

1. **Python 3.10+**：安装时记得勾选 Add to PATH
2. **FFmpeg**：配好环境变量，命令行能直接认
3. **素材目录**：输入输出分开，别混在一起

> **先备份，再自动化。** 脚本再快，也补不回来被覆盖的原片。

## 我是怎么跟 Codex 说需求的

不用写代码，就像跟同事交代活儿一样：

```
帮我写一个 Win 11 批处理流程：
1. 读取 D:\raw\ 下的所有 mp4
2. 每段去掉前 5 秒片头、末尾 3 秒结尾
3. 统一转成 1080p H.264，输出到 D:\cut\
4. 每个文件处理完打印日志
5. 先只用 1 个文件试跑，确认结果后再全量执行
```

它会把步骤列出来，再写脚本。**别让它一上来就全量跑**，先小范围试，确认没问题再说。

## 我现在的固定流程

1. **说清楚素材在哪、规则是什么、输出到哪**
2. **让它先列操作步骤**，我确认没问题再碰文件
3. **先用 1 个文件试跑**，看时长、画质、日志
4. **没问题再全量跑**
5. **收尾时人工过一遍成片**，再归档或发布

![Win 11 准备清单](images/pic2.png)

## 我为什么不迷信“一键跑完”

我不太信“一键跑完”这种说法，不是 Codex 不行，而是我的素材和需求会变。今天这批是横屏，下批可能就变竖屏；这周要 1080p，下周可能又要改码率。

所以我坚持一个原则：**自动化帮我跑完脏活，人工收尾判断效果。**

## 翻车现场，都是真事

先说没备份那次。有一回我没把输入输出目录分开，脚本参数又写错，结果把几个原片覆盖了。虽然事后从回收站救回来一部分，但那天晚上我基本没睡。

再说转码那次。我让 Codex 一口气处理三十多个文件，笔记本风扇狂转，电池掉得飞快。后来我学乖了：**批量任务先限制并发数**，比如一次只转两个，插着电源，挑晚上跑。

还有一次画面糊掉。输出虽然还是 1080p，但画质明显发虚。我自己看不出问题在哪，就让 Codex 把每条命令的参数打出来，再拿一个片段反复试，才发现是缩放方式写错了。**先拿单个文件验证画质，再放心全量跑。**

这些坑不算高级，但踩一次就能让你记很久。

## 给新手的避坑清单

这几个细节看起来小，但容易让人折腾一晚上：

| 坑 | 对策 |
| --- | --- |
| 硬盘空间不够 | 批量处理前先看剩余空间，预留两倍素材大小 |
| 笔记本转码中休眠 | 电源设置改成不睡眠，或插电运行 |
| 杀毒软件拦截 | 把素材和输出目录加入信任，或让 Codex 用官方工具 |
| 路径有中文或空格 | 让脚本用引号包住路径 |
| 原片被覆盖 | 输入输出目录分开，保留备份 |

> **自动化不是无人值守。** 我头一回全量跑的时候还是盯着看，确认输出目录里的文件数量和时长都对，才敢收工。

## 适合什么人用

我试下来，这套玩法大概率适合这样的场景：素材多、规则固定、愿意把需求写清楚，也愿意先试跑再全量。

不太适合的场景也有：追求精细转场和调色、一点命令行都不想看、素材乱到文件名毫无规律的人。遇到这种情况，还是先把素材整理好，再谈自动化。

![适合什么人用](images/pic3.png)

## 收个尾

说白了，Codex 不神奇，就是把我烦的重复活接过去了。你要做的，是把需求说清楚，然后盯着它别乱来。

这篇算是我折腾自动化剪辑的起点，后面如果跑通了更稳的流程，我再发出来。

我是精神小哥的思绪。感兴趣可以点个关注，下次接着聊。
